spring:
  application:
   name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/oauth-practic
    username: postgres
    password: Operator332@@
    driver-class-name: org.postgresql.Driver
  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: secret
    stream:
      virtual-host: vhost
eureka:
  client:
    fetch-registry: false
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka/
  instance:
    ip-address: true
server:
  port: 8081